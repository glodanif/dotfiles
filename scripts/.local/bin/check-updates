#!/bin/zsh
# check-updates — show pending Void Linux package updates

output=$(sudo xbps-install -Sun 2>&1)
if [[ -n "$output" ]]; then
    echo "$output"
else
    echo "All packages are up to date."
fi
