#!/bin/zsh
# The pause lives here, once: every sys-* script it launches must NOT call
# press_any_key itself, or a badge click prompts twice.
cmd="$*"
ghostty \
  --title="${cmd} - float" \
  -e zsh -c "source \$HOME/.config/bash/utils.sh; ${cmd}; echo; press_any_key"

