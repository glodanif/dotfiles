# No Hyprland plugins in use.
#
# Previously loaded hy3 (tabbed tiling layout) via hyprpm. Removed 2026-07-21
# because hy3 requires recompiling against every Hyprland release and did not
# build on Hyprland 0.56.0 (upstream hy3 issue #324). Now using the built-in
# dwindle layout — see look-and-feel.conf (layout + group{} tab styling) and
# keybinds.conf (native group/movewindow binds).
#
# dwindle auto-splits along a window's longer edge by default, covering what
# hy3's autotile (trigger_width/height) did.
